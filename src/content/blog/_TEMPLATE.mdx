---
title: "Post title, sentence case, under 80 characters"
description: "One or two sentences. Shows in the index, in the meta description, on the OG card and in RSS. Max 200 characters."
pubDate: 2026-01-01
# updatedDate: 2026-01-15
tags: ["ml", "healthcare"]
draft: true
# heroAlt: "Alt text if you add a hero image later"
---

{/*
  HOW TO USE THIS TEMPLATE
  1. Copy this file to src/content/blog/<slug>.mdx. The file name becomes the URL: /blog/<slug>/.
     Files starting with "_" are ignored by the loader, so this template never builds.
  2. Fill in the frontmatter above. pubDate is required; updatedDate and heroAlt are optional.
  3. Write in Markdown below. Headings start at ## (the page h1 is the title).
     Code fences get Shiki highlighting. Tables, blockquotes and lists are styled.
  4. Run `npm run dev` and open /blog/ to preview. Drafts show with a DRAFT badge in dev only.
  5. Set draft: false to publish. The next push to main deploys it, generates /og/blog/<slug>.png,
     and adds it to /rss.xml.
  PRIVACY: public page. No employer, dataset, project, course or professor names. See CONTENT.md.
*/}

## First section

Opening paragraph. First person, short sentences, concrete nouns.

```python
print("code blocks are highlighted")
```

## Second section

> Blockquotes look like this.

- Bullet one
- Bullet two
